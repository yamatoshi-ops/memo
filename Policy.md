ご指摘の通り、現在のW5以降は「状態を遷移させること」が主語になり、磁束マップ生成が従属しています。
工程表を、full CSV → fit → 残差 → 磁束マップ → 技術判断の直線的な成果物駆動へ組み直します。
検証計画の書き換えに当たるため、ローカルのtech-report方針も使います。
ただし、今回はGateを増やすためではなく、問い・必要データ・成果物を1対1にして余分な工程を削るために使います。
まず現行コードの実行入口と既存計画の依存を確認します。
